{
  "name": "@dsh-external/dsh-upstream-fixes",
  "description": "Compatibility fixes for broken third-party dsh plugins: shims dsh-sidechain's deep client import onto the public runtime client entry, aliases the legacy httpServer service for dsh-vision-toolkit, and gives the plugin-console panel real update support (git pull / pnpm update --latest).",
  "version": "0.5.0",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml"
  ],
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "immediately": true
    }
  },
  "license": "BSD-3-Clause"
}
